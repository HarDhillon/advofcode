file = open('day3.info.txt')
(wire1steps, wire2steps) = file.read().split()

wire1steps = wire1steps.split(',')
# wire 1 = [ R8 , U5 , D4 , L7 ]

wire2steps = wire2steps.split(',')

origin = [0,0]

wire1pos = [0,0]
wire2pos = [0,0]

# horizontal movement =  h
# vertical movement = v

for h,v in wire1pos:
    if
    print ('distance =', x[1:])

# make a tuple pair for each pos on first wire ( wire1 ) and then
#compare that to a tuple pair of each pos on the other wire ( wire2 )
